{
  "name": "zetatube",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && cd ../server && npm install && cd ../ && npm run build:server && npm run build:client",
    "build:server": "cd server && npm run build",
    "build:client": "cd client && npm run build",
    "start": "cd server && npm start",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}