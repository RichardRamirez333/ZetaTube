{
  "name": "zetatube",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npx --yes tsc -b && npx --yes vite build && cd ../server && npm install && npx --yes tsc && cd ..",
    "start": "cd server && npm start",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}